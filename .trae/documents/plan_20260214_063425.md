## 构建错误修复计划

### 1. 修复Agent导入问题 (4个文件)
- 修改 `ai.provider.service.ts` 导出 `AIProviderService`
- 更新 `outline-agent.ts`, `storyboard-agent.ts`, `storyline-agent.ts` 导入

### 2. 修复Prisma字段名不匹配 (15+错误)
- `video-generation.service.ts`: `duration` → `metadata` 中
- `scene-concept.service.ts`: `prompt` → `aiPrompt`
- `script-analysis.service.ts`: `result` → `metadata`
- `version-control.service.ts`: 使用正确的include语法

### 3. 修复Sentry API变更 (3个)
- `lib/sentry.ts`: `ProfilingIntegration` → `nodeProfilingIntegration`

### 4. 修复ZhipuProvider实现 (2个)
- `zhipu.provider.ts`: 将 `baseUrl` 改为 public
- 添加缺失的 `createVideo` 方法

### 5. 修复Queue类型问题 (15+错误)
- 安装正确的 `@types/bull` 或调整导入
- 使用动态导入或类型断言

### 6. 修复未使用变量 (20+)
- 删除或使用 `processingInterval`, `hash` 等变量

### 7. 修复其他导入问题
- `date-fns` 依赖
- `logger` 导出
- `MemberRole`, `ProjectType` 类型导出

预计修复后错误数量将大幅减少。