// 统一导出工具函数
import cn from './cn';
import { getColor, getShadow, getFontSize, getSpacing, createGradient, responsive } from './styles';

// 导出所有工具
export {
  cn,
  getColor,
  getShadow,
  getFontSize,
  getSpacing,
  createGradient,
  responsive,
};

// 导出默认值
export default {
  cn,
  getColor,
  getShadow,
  getFontSize,
  getSpacing,
  createGradient,
  responsive,
};